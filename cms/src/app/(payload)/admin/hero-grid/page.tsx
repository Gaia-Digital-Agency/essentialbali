import HeroGridView from "@/components/HeroGridView";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Hero Ad Grid — Essential Bali",
};

export default function HeroGridPage() {
  return <HeroGridView />;
}
